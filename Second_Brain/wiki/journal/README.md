# Per-User Journals

Each developer's LLM writes daily journal summaries in their own subfolder:

```
wiki/journal/{username}/YYYY-MM-DD.md
```

Create your folder automatically on first use. No manual setup needed.
