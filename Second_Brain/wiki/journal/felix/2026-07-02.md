**Author:** felix

# 2026-07-02

## [16:15] Scoped `searchWiki` to `wiki/fragments/` only

- Reviewed `.compiled/` structure/content vs source fragments — confirmed compiled
  pages carry no unique knowledge (mechanical assembly only).
- Fixed `searchWiki` (and `search-wiki.ps1`) to search `wiki/fragments/` and skip
  `README.md`, excluding the generated `.compiled/`, `log/`, and `journal/` trees.
- Redefined the optional `folder` arg to mean a fragments subfolder/author
  (the old `entities`/`concepts` examples never resolved).
- Rationale: determinism across machines, stop compiled hits suppressing
  auto-injection, remove duplicate/inflated hits, halve I/O.

## [17:40] Wiki search recall: OR fallback + target/tag weighting

- Added target (`+6`) / tags (`+3`) keyword weighting so curated frontmatter acts
  as synonyms and lifts the right pages.
- Added OR fallback: when strict AND matches nothing, return files matching any
  keyword, scaled by the fraction matched, so partial-vocabulary questions still
  surface the closest pages instead of getting nothing.
- Mirrored in `search.ts` and `search-wiki.ps1`. Tests: OR-fallback + weighting
  cases added; full second-brain surface 62 pass. Re-stamped two code anchors
  whose line numbers shifted from the edits.
- Tests: added author-folder + `.compiled`/README-exclusion cases; search,
  read-through, manifest-parity, and the omo-opencode second-brain tool/injector
  suites all pass.
