# Log — felix — 2026-07-30

## [12:00] code | Context-limit recovery now recognizes the `Input length (X tokens) exceeds the maximum allowed length (Y tokens)` inference-server error. Added numeric pattern `/input length.*?(\d+).*?tokens.*?exceeds.*?maximum.*?(\d+)/i` + keyword `"maximum allowed length"` to `parser.ts` (anthropic-context-window-limit-recovery), and the same keyword to `token-limit-detection.ts` (todo-continuation-enforcer). This makes `extractTokensFromMessage` return `current=141382,max=140534` so `isOverLimit` is true and the existing compression pipeline (Stage A aggressive truncation when `experimental.aggressive_truncation` on, then Stage B summarize) engages. Added 2 regression tests (string + object forms); parser suite 13 pass.

## [12:00] fragment | analysis context-window-limit-recovery (input-length-server-error) — documents the two-gate parser + two-stage compression flow and the new pattern/keyword.
