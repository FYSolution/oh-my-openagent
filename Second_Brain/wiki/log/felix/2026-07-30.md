# Log — felix — 2026-07-30

## [12:00] code | Context-limit recovery now recognizes the `Input length (X tokens) exceeds the maximum allowed length (Y tokens)` inference-server error. Added numeric pattern `/input length.*?(\d+).*?tokens.*?exceeds.*?maximum.*?(\d+)/i` + keyword `"maximum allowed length"` to `parser.ts` (anthropic-context-window-limit-recovery), and the same keyword to `token-limit-detection.ts` (todo-continuation-enforcer). This makes `extractTokensFromMessage` return `current=141382,max=140534` so `isOverLimit` is true and the existing compression pipeline (Stage A aggressive truncation when `experimental.aggressive_truncation` on, then Stage B summarize) engages. Added 2 regression tests (string + object forms); parser suite 13 pass.

## [12:00] fragment | analysis context-window-limit-recovery (input-length-server-error) — documents the two-gate parser + two-stage compression flow and the new pattern/keyword.

## [15:00] code | Preemptive compaction now reserves the model's completion budget when deciding to compact. Root cause of `custom/qwen3.6-27b-fp8` overflow (131072 limit): request fails at `input + 32000 completion > limit`, i.e. input > ~99072 (76%), but the trigger only fired at 0.78\*limit = 102236 input — too late, so the session overflowed and had to fall back to a 200k model. Fix in `preemptive-compaction-trigger.ts`: `usageRatio = (totalInputTokens + resolveReservedOutputTokens(...)) / actualLimit`, where reserved = `getModelCapabilities().maxOutputTokens` capped at `floor(limit/2)`; reserved=0 when unknown so existing (incl. Anthropic 1M) behavior is unchanged. Added 2 regression tests mocking `../shared/model-capabilities`; suite 18 pass. Unrelated full-dir failures (runtime-fallback race timing, git-conflict scan timeout) confirmed pre-existing/flaky (runtime-fallback 230 pass in isolation).

## [15:00] fragment | analysis preemptive-compaction (output-token-reservation) — documents the proactive trigger, the completion-budget bug, the fix, and its preconditions/boundaries.

## [16:30] investigation | Why the token error persisted after deploy: `custom/qwen3.6-27b-fp8` has NO `limit.context` in the user's opencode.json, so `provider-config-handler.ts` never caches a context limit → `resolveActualContextLimit` returns null → preemptive compaction silently skips (`unknown context limit`). New error was input-only (`input 152445 > context 131072`), so output-reservation can't help — only earlier compaction can. FIX (user config): added `"limit": { "context": 131072, "output": 32000 }` to the custom model in `~/.config/opencode/opencode.json` (validated JSON). Also identified the background-agent fallback storm (`fallback-retry-handler.ts` walks the hardcoded chain, reachability gate bypassed when connected-providers cache empty, and it ignores `model_fallback:false`) as a downstream symptom of the overflow — left as a separate open gap.

## [16:30] fragment | analysis preemptive-compaction (context-limit-config-precondition) — documents that non-Anthropic models need `limit.context` declared or compaction no-ops, plus the background fallback-storm secondary symptom.

## [17:30] code | Background-agent fallback now honors an explicit `model_fallback: false`. Root gap: `tryFallbackRetry` (background/subagent retry) was gated by NEITHER `model_fallback` nor `runtime_fallback` — it walked the agent's hardcoded default chain (gpt-5.4-mini-fast → … → gpt-5.4-nano), producing a 9-deep `Model not found` storm on a single-custom-provider setup. Since both flags DEFAULT to false yet background fallback is always-on, gating on `=== true` would break defaults; instead gate on explicit `model_fallback === false` (distinguishable because the field is `z.boolean().optional()`). Fix: `create-managers.ts` passes `disableModelFallback: pluginConfig.model_fallback === false` into `BackgroundManager`; the manager's `tryFallbackRetry` method (single choke point for all 5 sources) early-returns false when set. Added regression test in `atlas-subagent-fallback-retry.test.ts` (4 pass); manager suite 199 pass. Residual: reachability fail-open (`if (!connectedSet) return true`) in `fallback-retry-handler.ts` left as separate gap.

## [17:30] fragment | analysis background-agent (fallback-honors-model-fallback-flag) — documents the three fallback systems, why explicit-false is the right gate, the choke-point fix, and the residual reachability gap.
