# 2026-07-01 — felix

## [00:19] code | Finalized local web_search (prefer_local): removed debug output, fixed union type error via createWebSearchToolsRecord helper, verified end-to-end on real OpenCode

## [00:19] fragment | entity local-web-search (overview) + lesson local-plugin-testing (opencode-sandbox-load-path)

## [00:24] code | Follow-up: fixed biome replaceAll/RegExp.exec warnings in web-search-local files; deleted 3 scratch debug test files; rebuilt + reverified

## [00:26] fragment | concept local-dev-workflow (build-clean-deploy-sandbox) — developer guide for local build/deploy loop

## [10:40] code | Added fetch-path pagination (detectPaginationNextFromHtml) so pagination auto-follow works in fetch-only mode; 7 tests + typecheck green

## [10:40] fragment | entity local-web-search (pagination) — browser vs fetch detection, 3 strategies, volume caps

## [11:05] code | Normalization layer: single formatWebSearchResult formatter used by both direct tool and redirect hook; unified shape + MAX_RESULT_CONTENT_CHARS=2000 cap; 10 tests + typecheck green

## [11:05] fragment | entity local-web-search (output-normalization) — one canonical formatter for both local paths, remote Exa/Tavily out of scope

## [11:45] code | Implemented Freshness Envelope in Second_Brain wiki tooling: compile-wiki.ps1 (freshness state machine + anchor-hash drift detection + index Freshness column + Needs Verification section), search-wiki.ps1 (freshness/trust-weighted ranking + State column), new anchor-hash.ps1 helper; SCHEMA.md documented. Proved FRESH + DRIFTED detection on real anchors.

## [11:45] fragment | decision memory-freshness (freshness-envelope) + analysis memory-freshness (self-demonstrating code_anchors) + stamped 3 local-web-search fragments with last_verified/trust/ttl_days

## [12:15] design | Second_Brain runtime bridges: pure-TS second-brain-core (raw-fragment read, no pwsh/.compiled dependency), wiki_search/wiki_read tools, ContextCollector second-brain source, compaction re-injection, out-of-wiki web-cache — grounded in real integration points (ContextCollector.register, CompactionHookDependencies)

## [12:15] fragment | decision second-brain-runtime (architecture) — 5-increment plan (R1 core → R5 web-cache) with per-increment QA gates, anchored to collector.ts + session-compacting.ts

## [12:40] code | Made wiki frontmatter parsers (compile-wiki.ps1 + search-wiki.ps1) tolerant of multiline flow arrays — a formatter had reflowed code_anchors across lines, silently disabling drift detection (false FRESH). Merge continuation lines before parsing. Proved multiline drift detection works; re-stamped a legitimately-drifted analysis anchor.

## [12:40] design | Revised second-brain-runtime design per feedback: .compiled/ index is a regenerable cache with a shared contract; TS core is a second producer (read-through cache, regenerate-in-TS, optional persist); resolved manifest-shape open question toward pwsh/TS parity.

## [12:40] fragment | lesson wiki-tooling (frontmatter-parsing) — markdown formatters silently break single-line frontmatter arrays; parse tolerantly

## [13:05] design | R1 file-level plan for packages/second-brain-core: pure-TS port of compile-wiki.ps1 + search-wiki.ps1, mirroring hashline-core conventions. Module map, public API (loadIndex/rebuild/searchWiki/computeAnchorHash/freshnessOf), 7-commit slicing, and the pwsh↔TS \_manifest.json parity test (committed golden + injected-clock determinism so CI needs no pwsh). Cross-runtime parity risks enumerated (CRLF/BOM/array-filter/SHA256).

## [13:05] fragment | analysis second-brain-runtime (r1-implementation-plan) — anchored to anchor-hash.ps1 canonical algorithm

## [14:10] code | Implemented R1: packages/second-brain-core, a pure-TS port of compile-wiki.ps1 + search-wiki.ps1 + anchor-hash.ps1 (15 src modules, barrel index, no pwsh/.compiled dependency). Public API: loadIndex/rebuildIndex/persistIndex (read-through cache), buildManifest/evaluateFragments, searchWiki, readTarget/renderTargetPage, computeAnchorHash, freshnessOf, parseFrontmatter. Byte-parity guarded by a committed pwsh golden manifest + injected-clock determinism (now=golden.generated) so CI needs no pwsh. 28 tests green (incl. manifest-parity deep-equal, anchor golden 8b041f22, CRLF+BOM equivalence, same-day multi-author conflict, anchor-drift DRIFTED, STALE age detail). tsgo typecheck clean. Wired into root workspaces.

## [14:10] fragment | analysis second-brain-runtime (r1-implementation-plan → status: implemented) — records module map, parity strategy, and cross-runtime EOL/BOM handling as built

## [15:20] code | Implemented R2: `second_brain` config schema (enabled/path/search_default_top, off by default) + read-only `wiki_search`/`wiki_read` tools consuming second-brain-core, gated on `second_brain.enabled` via createSecondBrainToolsRecord in tool-registry-gated-tools.ts. wiki_search returns a freshness/trust-ranked markdown table; wiki_read returns the assembled per-target page. Added @oh-my-opencode/second-brain-core workspace dep to omo-opencode, regenerated assets/oh-my-opencode.schema.json. 5 new tool tests + config/registry suites green (21 pass), tsgo clean. NOTE: opencode-qa (live tool-exec proof) still required before this ships as a PR — bun-test + typecheck is the local gate only.

## [15:20] fragment | analysis second-brain-runtime (r2-tools) — wiki_search/wiki_read surface, config gating, and the remaining opencode-qa gate

## [16:20] code | Implemented R3: `second-brain` ContextCollector source. New pure `selectWikiContext` (searchWiki → resolve top hit's target → readTarget → truncate) in tools/second-brain/context.ts, and a `second-brain-injector` chat.message hook that intent-gates knowledge questions, reduces them to content keywords (searchWiki is strict AND — a full NL sentence never matches), and registers the top wiki page at priority "low"/source "second-brain" for the existing context-injector transform to inject. Added "second-brain" to ContextSourceType, wired the hook through createTransformHooks (gated on second_brain.enabled && inject_context !== false) → CreatedHooks → runChatMessageHooks. Added config fields inject_context (default true) + max_inject_tokens (default 1500), regenerated schema. 13 new tests + 45-test chat-message/hook suites green, tsgo clean. opencode-qa still the outstanding merge gate for R2+R3 (cannot run in Windows VS Code).

## [16:20] fragment | analysis second-brain-runtime (r3-context-injection) — selectWikiContext core, injector hook, AND-search keyword extraction, off-by-default gating

## [17:05] code | Implemented R4: pin auto-recalled wiki pages across compaction. New `SecondBrainPinStore` singleton (session→Set<target>) shared between the R3 chat.message injector (writer, now records each injected target) and a new `createSecondBrainCompactionInjector` (reader) that re-reads the CURRENT compiled pages via readTarget under the max_inject_tokens budget (removed targets drop out) and emits them under a "pinned across compaction" header. Added optional `secondBrainCompactionInjector` slot to CompactionHookDependencies (pushed into output.context after compactionContextInjector), wired via createContinuationHooks gated on second_brain.enabled && inject_context !== false. 12 new tests + compaction/continuation/plugin-module suites green (26 pass), tsgo clean. opencode-qa remains the outstanding merge gate for R2+R3+R4 (cannot run in Windows VS Code).

## [17:05] fragment | analysis second-brain-runtime (r4-compaction-reinjection) — pin-store singleton channel, readTarget re-read at compaction, CompactionHookDependencies slot, off-by-default gating, known session.deleted cleanup follow-up

## [17:45] code | Implemented R5: per-user web-cache for web_search. New `WebCache` (packages/omo-opencode/src/tools/web-search-local/web-cache.ts) — sha256 key over normalized query/goal/maxPages/maxDepth, TTL-bound get (evicts stale), atomic set of a `{trust:"source", cachedAt, ...}` entry under `.omo/web-cache/`, injectable clock, fs failures logged not thrown. Wired an optional `cache` into createLocalWebSearchTool (hit → formatWebSearchResult of cached CrawlResult; miss with >0 results → store) and built it in createWebSearchToolsRecord from new `websearch.local.cache.{enabled,ttl_minutes}` (default 60m, off by default, only when prefer_local). Regenerated schema. 5 new tests + 145 websearch/registry/config pass, tsgo clean. opencode-qa remains the outstanding merge gate for R2+R3+R4+R5 (cannot run in Windows VS Code).

## [17:45] fragment | analysis second-brain-runtime (r5-web-cache) — WebCache core (sha256 key, TTL evict, atomic write, trust:source), tool/registry wiring, websearch.local.cache config, off-by-default, redirect-hook path scoped out

## [18:20] code | Implemented R6 (auto-init): scaffold Second_Brain into a project so a client never sets it up by hand. New pure `scaffoldSecondBrain(root, {user?, now?})` in second-brain-core (creates wiki/fragments|log/{user}, raw/code-updates, .gitignore, minimal SCHEMA.md, seed overview fragment + daily log, then rebuildIndex to persist .compiled/; idempotent no-op when root exists; no pwsh dependency). Adapter `maybeAutoInitSecondBrain(directory, config)` gated on second_brain.enabled && auto_init, resolves user from SECOND_BRAIN_USER, failure-safe; wired at plugin startup right after loadPluginConfig. New `auto_init` config flag (default false), schema regenerated. 8 new tests + 161 core/plugin-module/config pass, tsgo clean both packages. opencode-qa remains the outstanding merge gate for R2-R6 (cannot run in Windows VS Code).

## [18:20] fragment | analysis second-brain-runtime (r6-auto-init) — scaffoldSecondBrain core, double-gated failure-safe adapter, startup wiring, cross-platform TS rebuildIndex (no pwsh), off-by-default
