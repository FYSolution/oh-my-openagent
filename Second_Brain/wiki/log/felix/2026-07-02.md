# Log — felix — 2026-07-02

## [01:27] R5/R6 local deployment fix + live QA (opencode 1.17.13 plugin resolution)

Continued the R1-R6 Second_Brain runtime bridge deployment. User reported the deployed local
build did not trigger web-search and did not init Second_Brain in a real OpenCode session
(screenshot showed the built-in "build" agent + WebFetch).

### Diagnosis

- opencode self-upgraded to 1.17.13 via npm (`2026-07-02T00:09:11Z`).
- 1.17.13 changed plugin resolution: bare `"plugin":["oh-my-openagent"]` is now silently
  ignored (managed store `~/.cache/opencode/packages` empty; no plugin-load step in
  `opencode.log`; omo log grew 0 bytes; agent fell back to "build").
- Separately, `~/.config/opencode/oh-my-openagent.json` had NO `second_brain` block, so R6
  auto-init was correctly gated OFF (expected, not a bug).

### Fix (runtime config only — no workspace code changed)

- `~/.config/opencode/opencode.json`: plugin -> `file:///C:/MyWorkplace/fy_solution/oh-my-openagent/dist/index.js`.
- `~/.config/opencode/oh-my-openagent.json`: added `second_brain:{enabled,auto_init,inject_context}`.
- Backups written (`.pre-fileurl.bak`, `.pre-sb.bak`).

### Live proof (opencode-qa)

`opencode run "reply READY"` from a fresh temp dir:

- Agent = "Sisyphus - ultraworker" (plugin loaded).
- `[second-brain-init] Scaffolded Second_Brain` + full tree created.
- `[tool-registry] Built tool registry` with 16 tools incl. web_search, wiki_search, wiki_read.
- `[websearch] prefer_local=true` + `[web-search-local] fetch-only mode enabled`.
- Evidence: `.omo/evidence/20260702-opencode-plugin-fileurl-fix/qa-evidence.md`.

### Follow-up

- Updated repo memory `/memories/repo/opencode-local-plugin-loading.md` with the corrected
  `file://` resolution (old bare-name + `.cache` sandbox note is now historical).

## [14:30] code | Second_Brain auto-init now MERGES into an existing (old-version) brain instead of no-op. `scaffoldSecondBrain` ensures missing dirs + absent top-level files + additive SCHEMA section-merge (never overwrite) + index rebuild, returns `{created,merged,added}`; `auto-init` drops the `existsSync` no-op; `config-merger` deep-merges the `second_brain` block across layers. Tests green (13 pass).

## [16:15] code | Scoped `searchWiki` + `search-wiki.ps1` to `wiki/fragments/` only (skip README.md; exclude generated `.compiled/`, `log/`, `journal/`). `folder` arg now = fragments subfolder/author. Fixes machine-nondeterministic results, compiled hits suppressing auto-injection, duplicate/inflated hits, ~2x I/O. Added author-folder + `.compiled`/README exclusion tests; search/read-through/manifest-parity + omo second-brain tool/injector suites green.

## [16:15] fragment | decision second-brain-runtime (search-scope) — searchWiki scans fragments only; rationale + code anchor search.ts#L73-L75@ae49bcb1.

## [17:40] code | Wiki search recall: OR fallback + target/tag weighting in `search.ts` + `search-wiki.ps1`. target hit +6, tags hit +3; when AND matches nothing, OR fallback returns any-keyword matches scaled by fraction matched. Added OR-fallback + weighting tests (full second-brain surface 62 pass). Re-stamped 2 anchors shifted by the edits (search.ts#L142-L144, search-wiki.ps1#L142-L152; hashes unchanged).

## [17:40] fragment | decision second-brain-runtime (search-recall) — OR fallback + tag/target weighting; anchor search.ts#L124-L128@1490baf4.
