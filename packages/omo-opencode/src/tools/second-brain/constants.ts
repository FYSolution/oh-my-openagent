export const WIKI_SEARCH_DESCRIPTION = `Search the project's committed Second_Brain wiki (team-shared project memory under Second_Brain/wiki). Keyword AND search: every space-separated keyword must appear in a fragment. Results are ranked by relevance, then weighted by freshness and trust, and returned as a markdown table (Score, State, File, Match). Use this FIRST to answer questions about project design, architecture, features, business logic, prior decisions, or lessons learned before falling back to code search.`;

export const WIKI_READ_DESCRIPTION = `Read the assembled Second_Brain wiki page for a single target/topic (for example "second-brain-runtime" or "local-web-search"). Returns the compiled page built from that target's fragments, with freshness recomputed from the current source. Use after wiki_search to get the full documented context for a topic. Lessons and decisions are not returned here; use wiki_search for those.`;
